function CriarTarefa() {
  document.getElementsByClassName("button").addEventListener('click');
  areaTexto = document.getElementByID("input");
  areaTexto.innerHTML = document.GetElementByID("texto-tarefa").textContent;
}

  CriarTarefa();
